#!/bin/bash
docker run -it --name devbox -v /Work/kyle/src:/opt/src kyledinh/devbox bash
say -v Karen Exiting... And removing the dev. linix. container.

# ENVIRONMENT
#  -e NODE_PATH=/opt/src/node_modules
